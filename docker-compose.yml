version: '3.8'

services:
  tada-api:
    image: tada-api:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - CUDA_VISIBLE_DEVICES=all
      - TADA_ENCODER_PATH=HumeAI/tada-codec
      - TADA_MODEL_PATH=HumeAI/tada-1b
      # Required to download the gated Llama models.
      # You can pass this at runtime: HF_TOKEN=your_token docker stack deploy ...
      - HF_TOKEN=${HF_TOKEN}
      # If you already downloaded Llama 3.2 1B manually, point to its mounted directory
      # - LLAMA_LOCAL_PATH=/models/models--meta-llama--Llama-3.2-1B/snapshots/...
      # HF_HOME is already set in the Dockerfile to /models, but you can override here
      - HF_HOME=/models
    volumes:
      # Mount a local directory to /models to cache the downloaded weights persistently
      - ./model_cache:/models
    deploy:
      mode: replicated
      replicas: 1
      # Example of how to add GPU reservation for Swarm
      # This requires nvidia-docker2 and swarm GPU support to be configured on the nodes
      # resources:
      #   reservations:
      #     devices:
      #       - driver: nvidia
      #         capabilities: [gpu]
      #         count: 1
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        monitor: 60s
        max_failure_ratio: 0.3
        order: stop-first
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
